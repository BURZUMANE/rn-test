import React from "react";
import { Text, View } from "react-native";

export const Test = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        color: "black",
      }}>
      <Text>Hello, world!</Text>
    </View>
  );
};
